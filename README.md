# tesla-stock-price-prediction
The objective of this project is to predict Tesla (TSLA) stock prices using historical time-series data. We model temporal dependencies using deep learning models, specifically SimpleRNN and LSTM, and compare their performance.  Target variable: Adj Close (Adjusted Closing Price) 
